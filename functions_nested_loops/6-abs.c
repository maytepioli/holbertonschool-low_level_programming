#include "main.h"

/**
 * _abs - que calcule el valor absoluto de un número entero
 * @n: numero
 * Return: 0
 */
int _abs(int n)
{
		if (n >= 0)
	{
			return (n);
	}

		else
	{
			return (-n);
	}
}
