#include "main.h"

/**
 *_strlen - la funcion que cuenta los caracteres
 *@s: string
 * Return: 0.
 */
int _strlen(char *s)
{

	int len;

	for (len = 0; s[len] != '\0'; len++)
	{
	}

	return (len);
}
