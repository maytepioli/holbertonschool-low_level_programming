#include "main.h"

/**
 * rev_string -
 *@s: strint
 * Return: Always 0.
 */
void rev_string(char *s)
{	
	int largo;


	for (largo = 0; s[largo] != '\0'; largo++)
	{
		if (largo/2)

			break;
	}
	
		

		_putchar('\n');
}	
